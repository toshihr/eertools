LIST (APPEND eertools_SOURCES eertools.cpp)
ADD_LIBRARY(eertools SHARED ${eertools_SOURCES})
SET_TARGET_PROPERTIES(eertools PROPERTIES VERSION ${VERSION} SOVERSION ${SOVERSION})

INSTALL(TARGETS eertools LIBRARY DESTINATION lib)
INSTALL(FILES eertools.h DESTINATION include)
